{
  "name": "idle-hands",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A document-inspection idle game where boredom is the antagonist and betrayal is the content.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "tone": "^14.8.49"
  }
}